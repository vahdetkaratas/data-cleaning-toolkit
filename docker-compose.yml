# Run the app:  docker compose up -d
# Logs:         docker compose logs -f
# Stop:         docker compose down
version: "3.8"
services:
  app:
    build: .
    container_name: data-cleaning-toolkit
    restart: unless-stopped
    ports:
      - "8501:8501"
